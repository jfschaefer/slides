\input{slides/montague-fragments-visual.tex}

\input{slides/fragments-need-prototyping.tex}

\begin{frame}[fragile]
    \frametitle{NLU Prototyping}
%     \begin{lstlisting}[mathescape,keywords=translate,keywordstyle=\bf,
%                        morestring={[d]"}, stringstyle=\it,showstringspaces=false]
    \begin{center}
\fbox{\parbox{\textwidth}{{\ttfamily
> \textbf{translate} {\small\color{nlfont}"Every student paints and is quiet."}\\
{\color{logicfont}$\forall x.s(x)\Rightarrow(p(x)\wedge q(x))$}\\[1.5em]
> \textbf{answer} {\small\color{nlfont}"Every student is quiet. John is a student. Is John quiet?"}\\
{\color{logicfont}$\forall x.s(x)\Rightarrow q(x), s(j) \vdash^? q(j)$}\\
\color{logicfont}yes
}}}
    \end{center}

%     \end{lstlisting}
    \vspace{1.5em}
    \begin{itemize}
        \item Traditionally done in Prolog/Haskell
        \begin{itemize}
            \item[$\raa$] requires a lot of work
        \end{itemize}
            \item A dedicated framework might be better
        \begin{itemize}
            \item[$\raa$] only partial solutions exist
        \end{itemize}
        \item Can we combine existing partial solutions? % \com{Research Question}
        \begin{itemize}
            \item[$\leadsto$] GLIF
        \end{itemize}
    \end{itemize}
\end{frame}

